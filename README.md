
  # Redesign Medical Interface

  This is a code bundle for Redesign Medical Interface. The original project is available at https://www.figma.com/design/yipH8WRcFHFgP0j894RfKT/Redesign-Medical-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # v2
